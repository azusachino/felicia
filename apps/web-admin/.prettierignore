dist
node_modules

# ADMIN-01.8 E2E (Playwright/Node, not this app's Vite/browser build) — kept
# out of `make validate`'s format check the same way it's kept out of
# eslint.config.js's scope; test-admin-e2e is a separate, local-only target.
e2e
playwright.config.ts
playwright-report
test-results

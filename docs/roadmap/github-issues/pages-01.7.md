Run the Pages workflow against the real repository artifact.

## Scope

- Dispatch or merge `.github/workflows/pages.yml`.
- Verify project-site base path, `.json` requests, media, and design hashes.
- Document required Pages settings and record the deployed URL.

## Acceptance

A clean runner publishes a browsable artifact without secrets or local services.
